运行后发现两个问题，请逐一排查：

【问题1：NullReferenceException】
报错位置：BattleManager.ExecuteSkill(UnitData attacker, SkillData skill, UnitData target) 
at BattleManager.cs:411

请：
1. 展示第411行及前后20行代码
2. 分析哪个对象为 null（attacker？skill？target？skill内部字段？）
3. 重点检查：skill.validTargetPositions 是否忘记在 GameManager 
   的技能数据里赋值（新加的字段默认是 null 而不是空数组）
4. 修复：所有技能的 validTargetPositions 确保初始化为 new int[]{}
   而不是 null，ExecuteSkill 里加 null 检查保底

【问题2：目标选中逻辑异常】
现象：点击技能后，黄圈出现在第3个单位（视觉上像敌方位置），
但应该预选第1个合法目标

请：
1. 展示 EnterTargetingMode() 完整代码
2. 检查"合法目标列表"的计算逻辑：
   - IsValidTarget() 是怎么判断敌方/友方的？
   - 位置编号和实际战场位置是否对应正确？
   - 自动预选的是 validTargets[0]，这个单位是谁？
3. 展示当前战场上 playerUnits 和 enemyUnits 的排列顺序

先解决问题1的崩溃，再修问题2的逻辑。