# 任务2：核心架构重构 — 命令队列 + 三层属性

**优先级**：P0（架构基石）  
**预估工时**：7-10天  
**依赖**：无（可独立于任务1并行开发）

---

## 目标

按照《数组设计全面评估与重构报告》，将当前"扁平属性 + switch技能执行"的架构重构为"三层属性 + 原子命令队列"架构，为后续所有内容扩展奠定基础。

---

## 任务清单

### 2.1 三层属性结构重构

- [ ] 定义 `PrimaryAttributes`（VIT, STR, AGI, INT）
- [ ] 定义 `DerivedAttributes`（HP, SPD, ACC, DOD, DEF — 由Primary + 职业系数公式计算）
- [ ] 定义 `Modifier` 结构（type: add/mul, target, value, source）
- [ ] 定义 `ModifierAttributes`（统一的可遍历修饰器列表）
- [ ] 重构 `UnitData`：将扁平属性迁移为三层结构
- [ ] 添加 `FinalAttributes` 运行时计算（Primary → Derived → 叠加Modifiers）
- [ ] 更新所有引用属性计算的代码（伤害公式、行动顺序等）

**公式参考**：
```csharp
// 派生属性计算示例
MaxHP = VIT * 5 + classBaseHP[unitClass];
SPD = AGI * 2 + classBaseSPD[unitClass];
ACC = 80 + AGI * 2;
DOD = AGI * 1.5;
DEF = 0; // 来自装备/Modifiers

// 最终属性 = 派生值 + 所有 Modifiers 累加
```

### 2.2 原子命令队列系统

- [ ] 定义 `CommandType` 枚举：
  - `DealDamage` — 造成伤害
  - `ApplyStatus` — 施加状态
  - `RemoveStatus` — 移除状态
  - `Heal` — 治疗
  - `Shield` — 护盾
  - `MoveTarget` — 位移目标
  - `MoveSelf` — 自身位移
  - `ModifyAttribute` — 属性修改
  - `Summon` — 召唤
  - `ConsumeResource` — 消耗资源（压力/体力等）
- [ ] 定义 `Command` 基类 + 每个原子命令的具体实现
- [ ] 定义 `SkillData` 重构：`commands` 替换旧的 `skillEffectType` switch
- [ ] 实现 `CommandExecutor`：按顺序执行命令队列
  - 命令间严格顺序
  - 目标死亡时跳过后续命令
  - 返回执行结果（伤害数值/状态列表）
- [ ] 重构 `BattleManager.ExecuteSkill()`：调用 CommandExecutor 替代 switch

### 2.3 技能数据迁移

- [ ] 将现有6个技能（盾击、嘲讽、援护、精准射击、标记、弹幕覆盖）迁移为命令队列格式
- [ ] 在 `SkillDatabase` 或配置文件中定义所有技能的命令队列
- [ ] 验证每个技能执行结果与迁移前一致

**技能迁移示例**：
```csharp
// 盾牌猛击（旧：switch效果 → 新：命令队列）
"shield_bash" = new SkillData {
    commands = new List<Command> {
        new DealDamageCommand { baseDamage = 15, attrCoeff = ("STR", 1.0) },
        new ApplyStatusCommand { statusId = "stun", duration = 1, target = TargetType.Selected }
    }
};
```

### 2.4 对象池优化（可选P1）

- [ ] 为 `Command` 对象实现对象池
- [ ] 战斗开始时预分配命令对象
- [ ] 战斗结束后回收

### 2.5 验证标准

- [ ] 所有6个技能在新架构下执行结果完全一致
- [ ] 三层属性计算正确（手动验算3组数值）
- [ ] Modifiers 能正确叠加到最终属性
- [ ] 命令队列顺序执行正确（状态在伤害后施加）
- [ ] 目标死亡后跳过逻辑正常工作
- [ ] 现有测试战斗流程完整通过
