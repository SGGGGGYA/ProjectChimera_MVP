# 《Project Chimera》数值体系讨论 · 系统性总结

> **文档状态**：设计讨论记录（存档）。核心概念（减法公式、三层属性、源-类型乘法模型）已实现或部分实现。与代码不一致处见《核心数值框架文档.md》的勘误表。

## 一、 对话范围与阶段

本次对话涵盖了从“核心数值框架构建”到“底层数组结构审查”两个递进的专业阶段。以下按逻辑模块进行总结。

---

## 二、 主要数值概念与关键问题

### 2.1 核心数值框架构建

| 讨论模块 | 关键问题 | 达成的设计方案 |
|:---|:---|:---|
| **公式设计目标** | 如何让数值服务于“残酷养成 + 高策略战斗”体验？ | 确立五大原则：支持多层博弈、情感张力、可量化战术胜利、长线养成满足、边际效用递减 |
| **基础伤害公式** | 用减法还是除法？ | 采用**减法公式**：`(技能基础伤害 × 力量系数 + 武器攻击力) × 伤害修正 - 目标防御`，配合±10%浮动和1点保底伤害 |
| **属性面板** | 需要哪些属性？ | HP、VIT、STR、AGI、INT、SPD、DEF、ACC、DOD、压力抗性 |
| **叠加契约** | 百分比增益如何叠加才不会膨胀？ | 确立**“源-类型”双维乘法模型**：不同“源（天赋/地形/怪癖/装备/Buff）”之间**乘法叠加**，同源内部**加法叠加** |
| **成长曲线** | 炮灰如何成长为英雄？ | 指数经验曲线（`100 × 等级^1.5`）；升级只增加主属性；次级属性（SPD/ACC/DOD）仅靠装备/怪癖/天赋提供；“命名时刻”触发5%全属性永久跃升 |
| **压力系统** | 压力如何产生、如何缓解？ | 战斗中暴击/队友濒死等事件产生压力；首都休整每日-5，酒馆消费每日-15；100满值触发美德/折磨判定 |
| **战斗时间消耗** | 宏观天如何与战斗接轨？ | `0.2 +（敌方总等级/100）`天，将时间变为战略资源 |
| **特殊场景** | 绝境如何翻盘？ | 战力比<1:1.5时激活“绝境意志”：每受击5%×次数概率触发伤害减半+压力-15 |
| **AI战斗** | AI势力间战斗如何高效处理？ | 用`战斗力(BR)`公式快速结算，不运行完整战斗逻辑 |

### 2.2 数组与数据结构审查

| 审查对象 | 诊断的问题 | 重构方案 |
|:---|:---|:---|
| **单位属性数组** | 扁平键值对导致数据爆炸、衍生值硬编码不一致 | **三层属性结构**：核心属性(Primary) → 派生属性(Derived，公式计算) → 修饰属性(Modifiers，统一增益列表) |
| **技能效果数组** | 混合效果执行顺序混乱、目标失效处理不明、扩展需硬编码 | **顺序命令队列(Command Queue)**：技能由原子命令(deal_damage, apply_status, move_unit等)按顺序组成，前序完成再执行后续，可自由组合 |
| **怪物掉落数组** | 固定列表导致“刷固定怪”、无惊喜、与游戏状态脱节 | **基于标签的智能掉落**：道具标签化，怪物定义`tag_filter`+`rarity_range`的生成请求，运行时动态抽取；接入保底逻辑 |
| **天赋/地形修饰符叠加** | 全加法叠加导致伤害翻倍膨胀 | **“源-类型”双维乘法契约**（与框架部分一致，进一步形式化） |
| **美德/折磨判定池** | 随机抽取无叙事逻辑，英雄因何而疯无理由 | **上下文感知抽取**：为压力事件和美德/折磨条目打标签，触发满值时优先筛选标签匹配的条目（高叙事性池），再混合普通池按权重抽取 |
| **数据存储效率** | JSON加载慢、字符串ID浪费内存、战斗命令频繁创建销毁 | 强制要求：配置表用FlatBuffers二进制序列化；ID用整数索引；战斗命令队列使用对象池 |

---

## 三、 达成的主要共识

1.  **设计驱动平衡**：所有数值公式和数组结构，优先服务于“单人故事生成器”这一核心愿景，而非追求竞技公平。
2.  **数据定义玩法**：技能效果通过“原子命令队列”定义，掉落通过“标签请求”定义，美德/折磨通过“叙事标签”定义。内容是挑战，玩法由数据驱动。
3.  **“源-类型”乘法模型**：作为游戏百分比增益的顶层契约，可从根本上遏制数值膨胀。
4.  **分层与解耦**：无论是属性（三层结构）还是系统间通信（事件驱动），都必须避免硬编码和强耦合，以确保长期扩展性。
5.  **性能即平衡**：手机平台的存储格式（FlatBuffers）、ID索引、对象池等技术要求，是数值策划必须同步考虑的约束，而非纯技术问题。
6.  **边际效用递减**：所有百分比类次级属性（闪避、暴击、压力抗性）必须设定有效范围和极值限制，收益曲线在超限后平缓化。

---

## 四、 存在的分歧

在本次对话范围内，**未出现明确的设计分歧**。讨论流程为：你提出爽点与玩法融合 → 我提供方案建议 → 你确认选择（如战斗场景选A、英雄系统选混合模式、开局自选领袖）→ 我深化并扩展至完整技术方案。所有选择均已转化为文档中的设计决策记录。

---

## 五、 尚未解决或建议继续深入的问题

以下问题在讨论中被标记为“后续深入”，尚未最终定案：

| 待深化项 | 当前状态 | 建议 |
|:---|:---|:---|
| **具体职业/技能的技能树与原子命令列表** | 框架已定，内容待填充 | 下一步优先设计战士、游侠、学者三个核心职业的完整命令队列 |
| **科技/文化树的节点结构与解锁物的数值映射** | 已提及概念 | 需用DAG（有向无环图）设计，并定义每个节点的属性增益与消耗 |
| **英雄等级上限（冒险者10/领袖15）是否合适** | 已设初值 | 需通过Excel模拟验证后期是否出现数值断层 |
| **“源”的分类是否足够完备** | 已定义源A-E | 加入“神器/终局装备”源时需重新评估乘法叠加是否有溢出风险 |
| **完整数值配置表（GameBalanceData）的具体参数** | 框架已定义 | 进入实现阶段后，所有公式中的系数、上限值需生成完整的默认值 |

---

## 六、 当前交付物清单

本次对话产出的核心文档：

1.  **《游戏设计技术规格书 V2.0》**：涵盖项目愿景、系统架构、核心循环、模块规格、接口定义、风险与里程碑。
2.  **《核心数值框架文档 V1.0》**：涵盖公式目标、属性面板、伤害公式、成长曲线、压力/时间/资源联动、参数范围、测试标准、特殊场景适配。
3.  **《数组设计评估与重构报告 V1.0》**：涵盖属性数组分层、技能命令队列、标签化掉落/叙事抽取、数据存储效率优化。

---

*以上文档共同构成了《Project Chimera》从设计理念到开发落地的完整数值与技术基线。后续所有内容迭代将以此为标准。*