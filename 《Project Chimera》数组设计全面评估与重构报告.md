> **文档状态**：设计评审报告（存档）。大部分建议已采纳并在代码中实现（三层属性结构、命令队列、StatType 枚举）。留存的未实现建议标记为 `[N/A]`。

好的，作为项目数值策划，我将基于《核心数值框架文档 V1.0》和《技术规格书 V2.0》，对当前游戏系统中的**全部核心数组设计**进行系统性审查与重构。

---

# 《Project Chimera》数组设计全面评估与重构报告

**文档版本**：V1.0  
**关联文档**：核心数值框架 V1.0 / 技术规格书 V2.0  
**评估日期**：2026-05-05  

---

## 一、 审查范围与方法

此次审查覆盖游戏中所有以“数组/列表/集合”形式存在的数据结构，包括但不限于：
-   单位属性数组
-   技能效果集合
-   怪癖池与天赋表
-   地形修饰符集
-   掉落与奖励表
-   科技/文化树节点集
-   AI行为权重集

审查标准基于以下五个维度：
1.  **平衡性**：数组是否导致数值溢出或策略单一化。
2.  **扩展性**：新增内容是否无需重构核心数据结构。
3.  **体验**：数组结构是否支撑玩家爽点，而非阻碍。
4.  **数据逻辑**：数组间的引用关系是否清晰、一致、无环。
5.  **效率**：对手机平台的存储和调用是否友好。

---

## 二、 核心数组问题分析与重构方案

### 2.1 单位属性数组：从刚性到柔性

**原设计 (隐性定义)**
```yaml
attributes: { hp: 45, stress: 30, vit: 5, str: 8, agi: 4, int: 2, spd: 5, def: 10, acc: 90, dod: 5, stress_resist: 10 }
```
这是一个典型的扁平键值对结构。

**问题诊断**：
1.  **硬编码泛化**：每增加一个新属性（如“火焰抗性”、“毒素穿透”），就必须扩展所有单位的此结构，导致数据爆炸和大量冗余。
2.  **级联计算复杂**：`ACC`、`DOD` 等本应由主属性派生的数值，以硬编码形式存在。若基础属性变更，必须手动重算所有衍生值，极易产生逻辑不一致。
3.  **边界模糊**：`SPD`、`ACC` 等属性直接暴露在基础容器中，与其“次级派生”或“特殊来源”的设计定位不符。

**重构方案：分层属性结构**

为每个单位建立**三层属性机制**，清晰分离不同来源和性质的数值：

*   **Layer 1: 核心属性 (Primary Attributes)**
    仅包含能自由分配点数的基础属性。这是单位的“基因”。
    ```yaml
    primary:
      VIT: 5   # 体质
      STR: 8   # 力量
      AGI: 4   # 敏捷
      INT: 2   # 智力
    ```

*   **Layer 2: 派生属性 (Derived Attributes)**
    由Layer1和职业系数通过公式计算得出，不可直接修改。这是单位的“物理表现”。
    ```yaml
    derived:
      HP: "= (VIT * 15) + 职业基础HP"       # 公式引用
      SPD: "= (AGI * 2.5) + 职业基础SPD"
      ACC: "= 80 + (AGI * 2)"
      DOD: "= AGI * 1.5"
    ```

*   **Layer 3: 修饰属性 (Modifier Attributes)**
    所有由装备、怪癖、天赋、地形带来的增益。这是一个**统一的、可遍历的修改器列表**。
    ```yaml
    modifiers: []
    # 示例：
    # - { type: "add", target: "ACC", value: 10, source: "森林天赋" }
    # - { type: "mul", target: "DOD", value: 0.15, source: "高地地形" }
    ```

**调整影响**：
*   **平衡性**：提升。从根本上杜绝了“手动改错一个衍生值导致单位过强/过弱”的问题。
*   **扩展性**：极佳。新增属性只需扩展`modifiers`的`target`枚举值，无需改动数据结构。
*   **体验**：更透明。玩家加点时有清晰的预期，所有增益来源可在UI上以`modifiers`列表形式清晰展示。

---

### 2.2 技能效果数组：从混乱到有序

**原设计 (潜藏问题)**
技能效果通常容易被定义成一个混合数组，例如：`[伤害, 位移, 眩晕]`。

**问题诊断**：
1.  **执行顺序错乱**：先计算伤害再触发位移，还是先位移再计算伤害？若目标被位移后离开范围，原伤害是否还生效？原数组无法保证一致的执行顺序。
2.  **目标失效**：目标在被技能的一部分效果击杀后，剩余的Buff（如“击杀后回复生命”）应施加给谁？若处理不当，会导致数组索引越界或逻辑错误。
3.  **组合爆炸**：每新增一种效果（如“溅射”、“偷取Buff”），技能设计者都需要程序员为其硬编码一个独立的处理函数，无法自由地与现有效果组合。

**重构方案：顺序命令队列与效果对象化**

每个技能不是一个“效果数组”，而是一个 **“命令对象的有序队列 (Command Queue)”** ，每个命令对象都是独立的、可复用的小模块。

例如，一个“冲撞并击退”的技能，其数据定义变为：
```yaml
skill_charge:
  name: "野蛮冲撞"
  commands:
    - cmd: "move_forward"           # 命令1：自身向前移动1格
      params: { distance: 1 }
    - cmd: "deal_damage"            # 命令2：攻击目标
      params: { 
        base: 10, 
        attr_coeff: "STR:1.2",
        damage_type: "physical"
      }
    - cmd: "apply_status"           # 命令3：给自身施加Buff
      params: { 
        target: "self",
        status_id: "momentum",
        duration: 2
      }   
    - cmd: "move_target"            # 命令4：击退目标
      params: { distance: 2 }
```

**命令执行规则**：
*   **严格顺序**：队列中的命令必须按顺序执行，前一个命令完全结束后再开始下一个。
*   **目标继承与失效处理**：`cmd: "deal_damage"` 的对象是“当前选中的敌方单位”。若此命令导致目标死亡，后续的 `cmd: "move_target"` 将自动失效，但不会报错。
*   **可组合性**：设计者可自由组合 `deal_damage`, `apply_status`, `move_unit`, `summon` 等原子命令，创造无限可能的技能，无需程序员介入。

**调整影响**：
*   **扩展性**：革命性提升。内容创作者（你自己）获得了创造新技能的完全自由。
*   **平衡性**：更易控。每个原子命令的效果都可以独立调整，数值策划只需关注原子命令的数值，而不是一个个完整技能的数值。
*   **数据逻辑**：统一、严谨，从根本上避免了技能执行的时序Bug。

---

### 2.3 怪物与掉落数组：从死板到智能

**原设计 (典型问题)**
为一个怪物硬编码一个固定的掉落列表：`loot_table: ["破损长剑", "生锈头盔"]`。

**问题诊断**：
1.  **固定化**：玩家只会去刷掉落“最强装备”的那个特定怪物，地图上其他怪物成为摆设。
2.  **无惊喜感**：与游戏强调的随机性、探索性背道而驰。
3.  **逻辑断层**：无法感知游戏状态。高难度怪物和普通怪物在掉落上无差异。

**重构方案：基于标签的智能掉落生成器**

为所有装备、道具定义**标签 (Tags)**，怪物不再掉落具体物品，而是掉落一个 **“基于标签的随机生成请求”**。

*   **道具标签化**：
    ```yaml
    item_sword_01:
      name: "队长的阔剑"
      tags: ["装备", "剑", "物理", "T2"]
      rarity: 2
    ```

*   **怪物掉落定义**：
    ```yaml
    monster_orc_warrior:
      loot_request:
        - { 
            tag_filter: { include: ["T2", "武器"], exclude: [] },
            rarity_range: [1, 3],
            count: 1 
          }
        - { 
            tag_filter: { include: ["消耗品"], exclude: [] },
            count_range: [1, 3]
          }
    ```

*   **掉落生成流程**：
    1.  系统读取怪物的`loot_request`。
    2.  从全物品池中，筛选出所有符合`tag_filter`和`rarity_range`的物品ID。
    3.  随机抽取指定数量的物品。
*   **动态修正**：
    系统可接入“保底逻辑”：若连续N次抽取无“传奇”标签物品，则下次“传奇”标签的`rarity_range`下限提升。确保玩家运气不会过于极端。

**调整影响**：
*   **体验**：极大提升。玩家在任何一场战斗中都可能获得惊喜，因为掉落的“范围”是统一的，只是高品质范围出现的概率更低，这符合心理学上的“间歇性变量奖励”原则。
*   **扩展性**：新增道具只需按标签规范入库，无需修改任何怪物的掉落表。

---

### 2.4 天赋与地形修饰符的数组叠加：从混乱到契约

**原设计 (《数值框架 V1.0》中的描述)**
`伤害修正% = 1 + 地形加成% + 天赋加成% + 怪癖加成%`，且所有百分比“加法叠加”。

**问题诊断**：
1.  **加法叠加的隐患**：当加成来源增多时，加法叠加会导致收益线性扩张，极易触发数值膨胀。例如 `1+0.2(地形)+0.3(天赋)+0.5(怪癖) = 2.0`，伤害直接翻倍。
2.  **统一“源”的缺失**：若不加分类，未来加入“技能Buff”等新来源时，会被直接丢进加法池，进一步加剧膨胀。

**重构方案：“源-类型”双维乘法叠加契约**

建立明确的叠加规则，对百分比增益进行分类。

*   **规则定义**：
    1.  **增益来源维度**：所有百分比增益被划分为不同“源 (Source)”：
        *   **源A (天赋)**：角色地形天赋。
        *   **源B (地形)**：地形全局效果。
        *   **源C (怪癖)**：角色怪癖。
        *   **源D (Buff/Debuff)**：战斗中的临时状态。
        *   **源E (装备)**：装备提供。
    2.  **叠加逻辑**：
        *   同一“源”内部的多个同类增益，**加法叠加**。
        *   不同“源”之间的增益，**乘法叠加**。

*   **公式修正**：
    ```
    伤害修正 = (1 + 天赋加成总和) * (1 + 地形加成总和) * (1 + 怪癖加成总和) * (1 + Buff加成总和) * ...
    ```

*   **数值案例对比**：
    *   假设：`地形加成=0.2 (源B)`，`天赋加成=0.3 (源A)`，`怪癖加成=0.5 (源C)`。
    *   **旧规则 (全加法)**：`1 + 0.2 + 0.3 + 0.5 = 2.0`。伤害变为200%。
    *   **新规则 (源间乘法)**：`1.2 * 1.3 * 1.5 = 2.34`。看似更高，但关键在于边际控制。
    *   **极端情况推演**：若再增加一个装备(源E) 0.5。
        *   旧规则：`2.0 + 0.5 = 2.5` (成长感弱，继续加即可)。
        *   新规则：`2.34 * 1.5 = 3.51` (复合增长，但每个源的获取都极难，且价值明确)。

**调整影响**：
*   **平衡性**：革命性提升。通过控制每个“源”的内部加法池大小和获取难度，可以极其精确地控制角色的最终强度上限，彻底规避无限加法叠加导致的通货膨胀。
*   **玩家认知**：更清晰。玩家会策略性地去追求“不同源”的增益，以获取最大化的乘积效益，而不是无脑堆同一类加成。

---

### 2.5 压力与美德/折磨判定数组：从简单随机到叙事权重

**原设计**
压力100时进行一次随机判定，从固定池中抽取。

**问题诊断**：
单调的随机池无法反映“这个英雄为何而疯”。一个在墓穴中持续承压的英雄，为何会随机获得“恐血症”？应有逻辑支撑。

**重构方案：基于标签的上下文感知抽取**

*   **事件标签化**：为导致压力满值的“最后一根稻草”事件打上标签。例如：
    *   被暴击 -> `["冲击", "暴力"]`
    *   队友死亡 -> `["丧失", "恐惧"]`
    *   被困沼泽地形 -> `["环境", "泥泞"]`
*   **美德/折磨条目也标签化**：
    ```yaml
    affliction_claustrophobia:
      type: "折磨"
      name: "幽闭恐惧"
      tags: ["环境", "恐惧"]
      effect: "在封闭场景(如遗迹内)战斗中，每回合+5压力"
    ```
*   **抽取逻辑**：不再是等概率随机。系统会：
    1.  从池中筛选出**标签与当前事件标签有交集**的条目，作为“高叙事性候选池”，其权重设为10。
    2.  其余无关联的条目，作为“普通池”，权重设为1。
    3.  最终从这两池合并后的总池中按权重抽取。

**调整影响**：
*   **体验与叙事**：质的飞跃。你的英雄之所以疯，开始有了“符合故事”的理由。一个因队友死亡而崩溃的英雄，更可能获得“幸存者内疚”或“复仇之心”，而不是毫不相干的“贪食症”。这完美服务于你的“单人故事生成器”愿景。
*   **数据逻辑**：需要维护标签系统，前期设计成本稍高，但运行时逻辑清晰可控。

---

## 三、 数据存储与调用效率优化建议

针对手机平台，必须考虑内存和加载效率。以下是针对性的硬性要求：

1.  **配置表使用Google Protobuf或FlatBuffers**：
    -   **禁止**在运行时加载JSON或XML进行游戏计算。
    -   **要求**：所有策划配置的`Excel/CSV`，在打包时必须序列化为二进制格式（如`FlatBuffers`）。`FlatBuffers`允许无需解析、直接在二进制数据上读取数值，内存零开销，加载速度极快，是移动端游戏开发的工业标准。

2.  **怪癖/天赋/状态等静态数据采用ID索引，而非字符串**：
    -   **禁止**：在运行时单位数据中保存 `quirk_name: "森林之子"`。
    -   **要求**：改为 `quirk_id: 10032`。所有静态描述文本和效果，通过`quirk_id`在静态数据表中查询。
    -   **效率收益**：整数比较远比字符串比较快，且节省大量运行时内存。

3.  **战斗命令队列的内存池化**：
    -   战斗中每施放一个技能都会创建一个命令队列，频繁`new`和`delete`会导致手机发热和卡顿。
    -   **要求**：必须为技能命令队列实现**对象池**。战斗开始时预分配一组命令对象，战斗结束后回收。

---

## 四、 审查结论与行动项

**总体评估**：
原设计玩法和系统架构优秀，但核心数组的定义方式多为传统扁平键值对和硬编码集合，存在**严重的扩展性瓶颈、潜在的数值膨胀风险和模糊的叙事逻辑**。无法直接支撑一个长期、复杂的内容迭代计划。

**优先级最高的行动项**：
1.  **[P0] 执行属性分层重构**：所有开发工作以此数据结构为基石。
2.  **[P0] 实现技能命令队列系统**：这是创造丰富战斗内容的唯一可扩展路径。
3.  **[P1] 重构叠加契约为“源-类型”乘法模型**：在Beta测试前必须完成，否则后期数值会随内容增加而崩溃。
4.  **[P2] 实现基于标签的掉落与美德/折磨抽取**：这是将“优秀游戏”与“能自我叙事的伟大游戏”区分开的关键。

本报告通过后，所有后续模块开发需严格遵循以上数组规范。所有JSON配置文件的结构定义，应以本报告中的**重构方案**为最终模板。