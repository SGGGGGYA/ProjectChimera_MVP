# Project Chimera — 角色系统融合重构方案

参考《暗黑地牢》数据驱动 + 组件化设计，对当前角色系统进行融合迭代。

---

## 一、现状与目标对比

| 维度 | 当前系统 | 目标 (DD 融合) |
|:---|:---|:---|
| 职业定义 | `unitName` 字符串 switch | `ClassDefinition` ScriptableObject |
| 属性计算 | Primary → Effective → Stress | 基础 + 装备 + Quirk + Buff + Stress |
| 技能 | 命令队列 (6 种 Command) | 效果链 (EffectType) + 位置约束 |
| 压力 | `stress` (0~100) + 10 种崩溃 | 0~200 阈值 + 崩溃/美德二分 + 心脏病 |
| Quirk | ❌ 无 | 正/负面特质属性修饰器 |
| 装备 | ❌ 无 | 武器/防具 + StatMod |
| 配置 | C# 硬编码 | 数据驱动 (ScriptableObject) |

---

## 二、新架构总览

```
ClassDefinition (ScriptableObject)
  ├── baseStats               ← 职业基础属性模板
  ├── hpPerLevel[]            ← 每级 HP 成长
  ├── resistances             ← 异常抗性 (stun/bleed/blight/stress/move)
  ├── skillPool               ← 技能池 (可选的技能列表)
  ├── activeSkillSlots        ← 上阵技能数 (如 4 个)
  ├── breakdownPool           ← 精神崩溃池 (各职业不同)
  └── virtuePool              ← 美德池 (各职业不同)

HeroInstance (重构 UnitData)
  ├── classData               ← 引用 ClassDefinition (只读)
  ├── unitName, resolveLevel
  ├── primaryAttributes       ← 保留现有 struct (可扩展/加点)
  ├── currentHp, maxHp, shieldHp
  ├── stress (0~200), mentalState (Normal/Afflicted/Virtuous/HeartAttack)
  ├── quirks (List<Quirk>)    ← 新系统
  ├── equippedWeapon          ← 新系统
  ├── equippedArmor           ← 新系统
  ├── activeSkills (List<SkillData>)  ← 从 skillPool 选取
  ├── activeEffects (List<StatusEffect>)  ← 已有
  ├── rankPosition (1~4)      ← 新系统
  └── isDead, isCorpse

SkillData (强化)
  ├── validUserRanks          ← 新: 允许在哪几个位置释放 (如 {1,2})
  ├── validTargetRanks        ← 新: 能选哪几个位置的目标
  ├── canTargetSelf / canTargetFriendly
  ├── accuracyMod / critMod
  └── commands (List<Command>)  ← 保留现有命令队列

Command / SkillEffect (融合)
  └── EffectType 枚举: DAMAGE / HEAL / BLEED / BLIGHT / STUN / MOVE
                      / STRESS / STRESS_HEAL / BUFF / DEBUFF / GUARD / SHIELD
                      / PUSH / PULL / SWAP

Quirk (新系统)
  ├── id, localizedName
  ├── isPositive, isLocked
  └── effects (List<QuirkEffect>)
        QuirkEffect: stat, amount, isPercent

Equipment (新系统)
  ├── Weapon / Armor
  └── mods (List<StatMod>)
        StatMod: stat, amount, isPercent
```

---

## 三、属性最终计算管线

```
GetFinalStat(StatType stat):
    base      = classData.baseStats[stat] + primaryAttributes[stat]
    levelMod  = hpPerLevel[resolveLevel]     // HP 独有
    equipMod  = weapon.mods[stat] + armor.mods[stat]
    quirkMod  = quirks.Sum(q => q.GetMod(stat))
    buffMod   = activeEffects.Sum(e => e.GetMod(stat))

    additive  = base + equipMod
    multiplier = 1 + quirkMod + buffMod

    return additive * Max(0, multiplier) * StressModifier
```

与当前系统的关系：
- `primaryAttributes` + `baseStats` → 替换现有 `PrimaryAttributes` VIT/STR/AGI/INT
- `equipMod` + `quirkMod` → 替换现有 `AttributeModifier` 中的部分功能
- `buffMod` → 保留现有 `AttributeModifier` 的 buff/debuff
- `StressModifier` → 保留现有 `StressManager.GetStatModifier()`

---

## 四、压力系统升级

```
stress 0 ~ 200

AddStress(amount):
    stress = Clamp(stress + amount, 0, 200)

    if stress >= 100 && state == Normal:
        if roll < virtueChance:
            state = Virtuous (美德)
        else:
            state = Afflicted (崩溃)
            stress = 100  // 崩溃后回落到 100

    if stress >= 200:
        HeartAttack  // 心脏衰竭，死亡判定（可抵抗）

/// mentalState 影响 AI 行为
/// Afflicted: 随机崩溃行为（自私/被虐狂/自残等）
/// Virtuous: 全属性加成 + 压力免疫
```

与当前系统的关系：
- 当前 `stress` 上限 100 → 提升到 200
- 当前 `BreakdownState` 只有崩溃 → 增加美德分支
- 新增 HeartAttack 机制
- `breakdownPool` / `virtuePool` 挂到 `ClassDefinition` 上，不同职业可定制

---

## 五、数据驱动迁移路径

### 第一阶段（不动现有代码，新增 ClassDefinition）
```
ClassDefinition.cs (ScriptableObject)
  ├── 职业基础属性
  ├── skillPool 引用
  └── CreateAssetMenu

BattleSetup.CreateUnit():
  ├── 根据 unitName 匹配 ClassDefinition (不再 switch)
  └── UnitData.classData = 引用
```

### 第二阶段（Quirk + 装备）
```
Quirk.cs / Equipment.cs
  ├── 属性修饰器叠加层
  └── GetFinalStat() 管线集成
```

### 第三阶段（压力升级）
```
StressManager.cs 重构
  ├── 阈值 0~200
  ├── 美德/崩溃二分
  └── HeartAttack
```

### 第四阶段（位置系统）
```
UnitData.rankPosition
SkillData.validUserRanks / validTargetRanks
  └── TurnManager 集成
```

---

## 六、保留的现有资产

| 保留 | 替换 |
|:---|:---|
| Command 命令队列 | `GameManager.GetDefaultSkills()` switch → ClassDefinition |
| CommandExecutor | `UnitData.GetClassBaseHP()` 硬编码 → hpPerLevel |
| StatusEffect | 字符串职业匹配 → classData 引用 |
| StressManager | DerivedAttributes 闲置 → 删除或接入管线 |
| BreakdownDatabase | PrimaryAttributes 直接字段 → 通过 StatType 枚举抽象 |
| AttributeModifier (buff) | |

---

## 七、影响范围

- `UnitData.cs` — 大改：新增 classData/quirk/equip/rank 字段，重构属性计算
- `GameManager.cs` — 中改：移除 CreateTestData/GetDefaultSkills switch，改为挂载 ClassDefinition
- `BattleSetup.cs` — 小改：CreateUnit 改为通过 ClassDefinition 初始化
- `StressManager.cs` — 中改：阈值 0~200 + 美德分支
- `TurnManager.cs` — 小改：集成 rankPosition
- `AttributeModifier.cs` — 保留，作为 buff 层
- `DerivedAttributes.cs` — 建议删除
